<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Профіль користувача</title>
    <style>
        body { font-family: sans-serif; }
        .container { max-width: 600px; margin: 50px auto; padding: 20px; border: 1px solid #ccc; border-radius: 5px; }
        h2 { color: #333; }
        p { margin-bottom: 10px; }
        strong { display: inline-block; width: 100px; }
        .logout-form { margin-top: 20px; }
        .logout-form button { background-color: #d9534f; color: white; padding: 8px 12px; border: none; border-radius: 4px; cursor: pointer; }
        .logout-form button:hover { background-color: #c9302c; }
    </style>
</head>
<body>
<div class="container">
    <h2>Профіль користувача</h2>
    <% if (user) { %>
        <p><strong>ID:</strong> <%= user.id %></p>
        <p><strong>Email:</strong> <%= user.email %></p>
        <p><strong>Ім'я:</strong> <%= user.firstName %></p>
        <p><strong>Прізвище:</strong> <%= user.lastName %></p>
        <p><strong>Вік:</strong> <%= user.age %></p>
        <p><strong>Роль:</strong> <%= user.role %></p>
    <% } else { %>
        <p>Дані користувача не завантажено.</p>
    <% } %>

    <form action="/logout" method="POST" class="logout-form">
        <button type="submit">Вийти</button>
    </form>
</div>
</body>
</html>